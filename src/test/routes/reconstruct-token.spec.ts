import request from "supertest";
import { expect } from "chai";
import createServer from "server";

const app = createServer();

describe("reconstruct token", function () {
    it("should have token in authorization header", function (done) {
 
    });
});